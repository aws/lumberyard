; this is an override config that should "add" the ps4 platform ; ACCEPTED_USE
; and "remove" the server platform.

[Platform ps4] ; ACCEPTED_USE
tags=console, renderer

[Platforms]
server=disabled
ps4=enabled ; ACCEPTED_USE

; this ONLY overrides the "ps4" value
; becuase this is being loaded on top of the defaults, this should LEAVE the others
; as they are.
[RC i_caf]
ps4=copy ; ACCEPTED_USE
